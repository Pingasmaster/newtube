# Newtube configuration (source of truth)

# Host path for media (bind-mounted into containers)
MEDIA_ROOT_HOST=./data/media

# Paths inside containers / manual installs
MEDIA_ROOT=/data/media
WWW_ROOT=/app/www

# Backend API settings
NEWTUBE_PORT=8080
NEWTUBE_HOST=0.0.0.0

# Public frontend port (Docker compose)
NEWTUBE_PUBLIC_PORT=8080

# Missing media behavior: 404 or prompt
NEWTUBE_MISSING_MEDIA_BEHAVIOR=404

# Optional: override the download_channel binary location (manual installs)
# NEWTUBE_DOWNLOAD_BIN=/usr/local/bin/download_channel
